<div class="main-panel">
          <div class="content-wrapper">
            <div class="page-header">
              {{!-- <h3 class="page-title"><a href="/admin/admin-addproduct"><i class="bx bx-right-arrow-alt"></i>Add New Products</a></h3> --}}
              <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
              
                </ol>
              </nav>
            </div>
            <div class="row">
              
            
              <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
               
                    </p>
                    <div class="table-responsive">
                      <table class="table table-dark">
                        <thead>
                          <tr>
                            <th>User name</th>
                            <th>Email</th>
                            <th>phonenumber</th>
                            {{!-- <th>price</th> --}}
                          </tr>
                        </thead>
                        <tbody>
                          {{#each allUsers}}
                          <tr>
                            
                            <td>{{this.name}}</td>
                            <td>{{this.email}}</td>
                            <td>{{this.phonenumber}}</td>
                            <td>
                              {{!-- <a class="btn btn-danger" href="/admin/delete-user/{{this._id}}" onclick="alert('the user will be deleted')">delete</a> --}}
                                {{#if this.blockUsers}}
          <a class="btn btn-warning"  href="/admin/unblock-user/{{this._id}}">unblock</a>
          {{else}}
          <a class="btn btn-warning" href="/admin/block-user/{{this._id}}">block</a>
          {{/if}}
                            </td>
                            
                          </tr>
                         {{/each}}
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
              
          </div>
  
  
  
  
  
  
  
  
  
  